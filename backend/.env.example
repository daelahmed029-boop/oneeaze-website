# ============================================
# OneEaze Backend Environment Configuration
# ============================================

# MongoDB Database Connection
# Get this from MongoDB Atlas (free tier)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/oneeaze?retryWrites=true&w=majority

# JWT Secret Key for Authentication
# Generate a random secret key (minimum 32 characters)
JWT_SECRET=oneeaze_super_secure_jwt_secret_key_2024_change_this_in_production

# Email Service Configuration (Optional)
# For sending welcome emails to waitlist users
EMAIL_SERVICE=gmail
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_app_specific_password_here

# Admin Dashboard Access Token
# This password is used to access the admin dashboard
ADMIN_TOKEN=oneeaze_admin_secure_token_2024_change_this

# Application Environment
# Use 'development' for testing, 'production' for live
NODE_ENV=production

# Server Port (Vercel will override this)
PORT=3000

# ============================================
# INSTRUCTIONS:
# 1. Copy this file and rename to '.env'
# 2. Fill in your actual values
# 3. NEVER commit '.env' to GitHub!
# ============================================

# MongoDB Setup:
# 1. Go to https://www.mongodb.com/atlas
# 2. Create free account
# 3. Build a cluster (Free M0 tier)
# 4. Create database user
# 5. Get connection string
# 6. Replace username, password, and cluster name

# Email Setup (Gmail):
# 1. Enable 2-factor authentication
# 2. Generate App Password
# 3. Use App Password in EMAIL_PASS

# Security Notes:
# - Use different secrets for production
# - Change all 'change_this' values
# - Keep this file private
